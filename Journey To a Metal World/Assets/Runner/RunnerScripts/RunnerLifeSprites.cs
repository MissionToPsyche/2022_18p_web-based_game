using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//unless I go and prepare an animation or sound effect (which might still go in the other file. 
// this file will be deleted)
public class RunnerLifeSprites : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
